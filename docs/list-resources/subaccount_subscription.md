---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "btp_subaccount_subscription List Resource - SAP BTP"
subcategory: ""
description: |-
  This list resource allows you to discover all subscriptions available for given subaccount.
---

# btp_subaccount_subscription (List Resource)

This list resource allows you to discover all subscriptions available for given subaccount.

## Example Usage

```terraform
# This feature requires Terraform v1.14.0 or later (Stable as of 2026)
# List resources must be defined in .tfquery.hcl files.

# Generic template for a list block
list "btp_subaccount_subscription" "<label_name>" {
  # (Required) Provider instance to use
  provider = provider_name

   config {
    # Provider specific filters
  }
}

# List block to discover all subscriptions for given subaccount
# Returns only the resource identities by default.
list "btp_subaccount_subscription" "all" {
  provider = btp

  # Required
  config {
    subaccount_id = "<subaccount_id>"
  }
}

# List block to discover all subscriptions for given subaccount with full resource details
# Setting include_resource = true returns full resource objects (e.g., state, app_name..)
list "btp_subaccount_subscription" "with_resource" {
  provider         = btp
  include_resource = true
  config {
  # Required  
  subaccount_id = "<subaccount_id>"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `subaccount_id` (String) The ID of the subaccount.
